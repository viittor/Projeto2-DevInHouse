import React from "react";
import Router from "../../routes";

const Map = () => {
    <Router/>
    return(
        <div style={{background: 'red'}}>
           Mapa
        </div>
    );
};

export default Map;